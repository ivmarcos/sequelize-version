const assert = require('assert');
const version = require('../lib');
const Sequelize = require('sequelize');

describe('sequelize-versionify', () => {

    it ('works', () => {

        assert.equal(true, true);

    })

});